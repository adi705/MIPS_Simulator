code to run the simulator:

python3 simulator.py name_of_the_.mem_file

to check if selection sort works remove the comments in simulator.py

Note: some of the files in the SRC have been reimplemented with a different name often starting with ‘my’ with the except for mux.py, add.py, memory.py every other essential file name begins with the word ‘my’. 